# Copy to config.env and edit before running from a remote machine:
#   cp config.example.env config.env
#
# TARGET — IP or hostname of the machine running pnpm dev:full (admin-panel :3000)
TARGET=192.168.0.89
PORT=3000
SCHEME=http
# GATEWAY_PATH=/gateway
# PAUSE=1.5
# KEEP_COOKIE=1
