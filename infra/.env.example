# Docker Compose reads apps/admin-panel/.env — not this file.
#
# Setup (once):
#   cp apps/admin-panel/.env.example apps/admin-panel/.env
#   # edit apps/admin-panel/.env
#
# Then:
#   cd infra && docker compose up --build
