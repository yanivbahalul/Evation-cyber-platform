## SHARED CONSTANTS

Shared by all students — change only with team agreement.

  trapTypes.js — TRAP_TYPES enum (SQLI, XSS, DATA_BOMB, HONEY_TOKEN, etc.)

Used by: gateway traps (Bar), telemetry (Max), admin dashboard labels (Yaniv).
