# Gateway Models

> **Owner:** Sagiv Levy

Local Mongoose model wiring for the **safezone** database on the gateway.

The canonical schemas live in [`../../../packages/db-schemas`](../../../packages/db-schemas) —
prefer those for new code so every service shares one definition.
