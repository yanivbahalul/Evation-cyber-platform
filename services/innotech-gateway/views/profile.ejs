<% const title = 'InnoTech — My Profile' %>
<%- include('partials/head', { title }) %>

<div class="container">
  <%- include('partials/topbar', { user }) %>

  <div class="layout">
    <%- include('partials/sidebar', { user, active: 'profile', adminPanelUrl }) %>

    <main class="card">
      <div class="hero">
        <div class="kicker">Employee</div>
        <h2 style="margin: 0 0 8px;">My Profile</h2>
        <div class="muted">Read-only view. Changes require HR approval.</div>
      </div>

      <div class="card-inner">
        <table class="table">
          <thead>
            <tr><th>Field</th><th>Value</th></tr>
          </thead>
          <tbody>
            <% profileRows.forEach(r => { %>
              <tr>
                <td><%= r.label %></td>
                <td><%= r.value %></td>
              </tr>
            <% }) %>
          </tbody>
        </table>

        <div style="height: 12px;"></div>

        <div class="btn-row">
          <button class="btn" type="button" id="edit-profile-btn">Edit details</button>
          <a class="btn primary" href="<%= withBase('/workspace') %>">Back to dashboard</a>
        </div>
      </div>
    </main>
  </div>
</div>

<script>
  (function () {
    var btn = document.getElementById('edit-profile-btn');
    if (!btn) return;
    var ticketUrl = '<%= withBase('/contact') %>?subject=' + encodeURIComponent('Profile update request') + '&source=profile_edit';
    btn.addEventListener('click', function () {
      alert('Changes require HR approval. Please open an IT ticket.');
      window.location.href = ticketUrl;
    });
  })();
</script>

<%- include('partials/foot') %>
