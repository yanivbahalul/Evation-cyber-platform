# Core API deps — ALWAYS installed. Lightweight so the image builds fast and the
# service runs fully (heuristic-only) out of the box with ML_ENABLE_MODELS=false.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# The heavy ML stack (torch, transformers, sentence-transformers) is optional and
# lives in requirements-ml.txt. It is only needed when ML_ENABLE_MODELS=true.
# Install it explicitly:  pip install -r requirements-ml.txt
# (In Docker, build with --build-arg INSTALL_ML=true, or set ML_ENABLE_MODELS=true
#  in infra/.env and rebuild — see infra/docker-compose.yml.)
