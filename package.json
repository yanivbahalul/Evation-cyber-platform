{
  "name": "innotech-monorepo",
  "private": true,
  "packageManager": "pnpm@11.0.8",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:ui": "pnpm --dir admin-panel dev",
    "dev:gateway": "pnpm --dir services/innotech-gateway dev",
    "dev:logging": "pnpm --dir services/logging-data-extraction dev",
    "dev:all": "dotenv -e admin-panel/.env -- concurrently -n ui,gateway -c blue,green \"pnpm --dir admin-panel dev\" \"BASE_PATH=/gateway PORT=4001 MONGODB_URI=$SAFEZONE_DB_URI pnpm --dir services/innotech-gateway dev\"",
    "dev:full": "dotenv -e admin-panel/.env -- concurrently -n ui,gateway,logging -c blue,green,magenta \"pnpm --dir admin-panel exec next dev -H 0.0.0.0 -p 3000\" \"BASE_PATH=/gateway PORT=4001 MONGODB_URI=$SAFEZONE_DB_URI pnpm --dir services/innotech-gateway dev\" \"pnpm --dir services/logging-data-extraction dev\"",
    "dev:full:clean": "pnpm --dir admin-panel clean && pnpm dev:full",
    "trap:demo": "bash scripts/yaniv-test/run-all.sh",
    "trap:chain": "bash scripts/yaniv-test/run-kill-chain.sh"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "dotenv-cli": "^8.0.0"
  }
}
