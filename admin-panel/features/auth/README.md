# Auth Feature

> **Owner:** Yaniv · **Mission 4.1** — Authentication & 2FA

The login, registration, and 2FA experience for Blue Team operators.

| Folder | What's inside |
|--------|---------------|
| [`components/`](components/) | `LoginPage`, `RegisterPage`, OTP enrollment UI |
| [`context/`](context/) | `AuthContext` — client-side session state |

Pairs with [`../../lib/auth`](../../lib/auth) and the
[`../../app/api/admin/2fa`](../../app/api/admin/2fa) routes.
