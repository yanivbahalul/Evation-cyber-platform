# Dashboard Context

> **Owner:** Yaniv · **Mission 4.2**

`SocketContext` — manages the `socket.io-client` connection and handles incoming
`liveAlert` events, updating React state so the dashboard re-renders without a refresh.
