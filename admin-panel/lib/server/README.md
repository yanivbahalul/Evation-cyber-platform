# Server Library

> **Owner:** Yaniv

Server-only helpers used by API routes and server components.

| File | Purpose |
|------|---------|
| `fetchDashboardData.ts` | Aggregates the stats bundle for the dashboard |
| `telemetryDb.ts` | Server-side access to the telemetry (malicious) DB |
| `adminDb.ts` | Server-side access to the admin/safezone DB |
