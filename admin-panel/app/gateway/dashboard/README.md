# Attack Monitor Page

> **Owner:** Yaniv

The Next.js page that hosts the live dashboard, assembling the
[`features/dashboard`](../../../features/dashboard) components.

Requires the `admin` role. The socket connects through Nginx at `/socket.io/` to Max's
telemetry service.
