# `/api/admin/honeytokens`

> **Owner:** Yaniv

Read and manage honey-token records. Tokens are issued by Bar's traps and stored in Max's
malicious DB; this endpoint surfaces them in the dashboard.

| Method | Purpose |
|--------|---------|
| `GET` | List honey tokens and their usage |
