# `/api/admin/users`

> **Owner:** Yaniv

Admin-user management, backed by the `AdminUser` schema (Sagiv).

| Method | Purpose |
|--------|---------|
| `GET` | List admin users |
| `PATCH` | Update a user (e.g. grant the `admin` role) |

See also [`reset-2fa/`](reset-2fa/).
